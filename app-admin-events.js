(function(){
  const statusLabel={pending:'Pending',confirmed:'Confirmed',completed:'Completed',cancelled:'Cancelled'};
  const costLabel={beverage_material:'Bahan minuman',bottle:'Botol',ice:'Ais',cooler_box:'Kotak gabus',delivery:'Kos penghantaran',packaging:'Packaging tambahan',other:'Kos lain'};
  const defaultPrice=q=>Number(q)>=600?3.30:Number(q)>=400?3.60:4.00;
  const sum=(r,k='amount')=>(r||[]).reduce((a,x)=>a+Number(x[k]||0),0);
  const fmtStatus=v=>statusLabel[v]||v||'-';
  const isoLocal=v=>{const d=new Date(v),z=n=>String(n).padStart(2,'0');return `${d.getFullYear()}-${z(d.getMonth()+1)}-${z(d.getDate())}T${z(d.getHours())}:${z(d.getMinutes())}`;};

  const oldBuildMenu=window.buildMenu;
  window.buildMenu=function(){oldBuildMenu();if(state.profile?.role!=='admin')return;const m=$('#sideMenu');if(m&&!m.querySelector('[data-view="events"]')){const b=document.createElement('button');b.dataset.view='events';b.innerHTML='<span>✦</span>Pengurusan Event';m.appendChild(b);}};
  const oldRenderAdmin=window.renderAdmin;
  window.renderAdmin=async function(view,root){if(view==='events')return adminEvents(root);if(view==='event-detail')return adminEventDetail(root,state.eventSelectedId);return oldRenderAdmin(view,root);};

  async function adminEvents(root){
    const f=state.eventFilters||{},search=f.search||'',start=f.start||'',end=f.end||'',status=f.status||'';
    const {data,error}=await state.supabase.rpc('admin_event_list',{p_search:search||null,p_start:start||null,p_end:end||null,p_status:status||null});if(error)throw error;
    const rows=data||[],income=sum(rows,'total_income'),received=sum(rows,'received'),balance=sum(rows,'balance_due'),cost=sum(rows,'total_cost'),comm=sum(rows,'total_commission'),profit=sum(rows,'net_profit');
    root.innerHTML=pageHead('ADMIN • EVENT','Pengurusan Event WAHH AIR','Tempahan Event/Wedding 100ml, kutipan, kos, komisen, stok digunakan dan pembahagian keuntungan. Data admin sahaja.')+`
      <div class="kpi-grid">${kpi('Jumlah Event',num(rows.length))}${kpi('Jumlah Pendapatan',money(income))}${kpi('Bayaran Diterima',money(received))}${kpi('Baki Pelanggan',money(balance))}${kpi('Kos Event',money(cost))}${kpi('Komisen / Upah',money(comm))}${kpi(profit>=0?'Untung Bersih':'Kerugian',`<span class="${profit>=0?'profit-positive':'profit-negative'}">${money(profit)}</span>`)}</div>
      <section class="panel"><div class="panel-head"><div><h3>Tambah Tempahan Event</h3><p>Harga lalai: 200–399 RM4.00 • 400–599 RM3.60 • 600+ RM3.30. Harga khas dibenarkan.</p></div></div>
        <form id="eventCreateForm" class="form-stack"><div class="grid-2">
          <label>Nama event / majlis<input id="evName" required></label><label>Nama pelanggan<input id="evCustomer" required></label><label>No. telefon<input id="evPhone"></label><label>Tarikh event<input id="evDate" type="date" value="${todayISO()}" required></label><label>Lokasi<input id="evLocation"></label><label>Kuantiti botol<input id="evQty" type="number" min="200" value="200" required></label><label>Harga / botol RM<input id="evPrice" type="number" min="0" step="0.01" value="4.00" required></label><label>Caj penghantaran pelanggan RM<input id="evDeliveryIncome" type="number" min="0" step="0.01" value="0"></label><label>Status<select id="evStatus">${Object.entries(statusLabel).map(([v,l])=>`<option value="${v}">${l}</option>`).join('')}</select></label><label>Kaedah kos<select id="evCostMode"><option value="combined">Gabungan bahan + botol</option><option value="itemized">Itemized / ikut penggunaan stok</option></select></label><label>Kos gabungan / botol RM<input id="evBaseCost" type="number" min="0" step="0.0001" value="0.74"></label>
        </div><label>Catatan<textarea id="evNotes" rows="3"></textarea></label><div class="muted">Mode gabungan menggunakan quantity × kos sebotol. Kos bahan/botol manual tidak boleh ditambah lagi supaya tidak double count.</div><button class="btn primary" type="submit">Simpan Tempahan Event</button></form>
      </section>
      <section class="panel"><div class="panel-head"><div><h3>Senarai Event</h3><p>Ringkasan ikut carian/penapis tanpa mengira event dua kali.</p></div></div>
        <form id="eventFilterForm" class="inline-form"><div class="field"><label>Cari</label><input id="eventSearch" value="${esc(search)}"></div><div class="field"><label>Dari</label><input id="eventStart" type="date" value="${esc(start)}"></div><div class="field"><label>Hingga</label><input id="eventEnd" type="date" value="${esc(end)}"></div><div class="field"><label>Status</label><select id="eventStatus"><option value="">Semua</option>${Object.entries(statusLabel).map(([v,l])=>`<option value="${v}" ${status===v?'selected':''}>${l}</option>`).join('')}</select></div><button class="btn ghost" type="submit">Tapis</button></form>
        <div class="table-wrap"><table class="data-table"><thead><tr><th>Event</th><th>Pelanggan</th><th>Tarikh</th><th>Status</th><th class="num">Botol</th><th class="num">Pendapatan</th><th class="num">Diterima</th><th class="num">Baki</th><th class="num">Untung</th><th></th></tr></thead><tbody>${rows.length?rows.map(r=>`<tr><td><strong>${esc(r.event_name)}</strong></td><td>${esc(r.customer_name)}</td><td>${esc(r.event_date)}</td><td>${esc(fmtStatus(r.status))}</td><td class="num">${num(r.quantity)}</td><td class="num">${money(r.total_income)}</td><td class="num">${money(r.received)}</td><td class="num">${money(r.balance_due)}</td><td class="num"><strong class="${Number(r.net_profit)>=0?'profit-positive':'profit-negative'}">${money(r.net_profit)}</strong></td><td><button class="btn sm primary event-open" data-id="${r.id}">Butiran</button></td></tr>`).join(''):tableEmpty(10,'Tiada event ditemui.')}</tbody></table></div>
      </section>`;
    $('#evQty',root).addEventListener('input',()=>$('#evPrice',root).value=defaultPrice($('#evQty',root).value).toFixed(2));
    $('#evCostMode',root).addEventListener('change',()=>$('#evBaseCost',root).disabled=$('#evCostMode',root).value==='itemized');
    $('#eventCreateForm',root).addEventListener('submit',async ev=>{ev.preventDefault();const b=ev.submitter;setBusy(b,true,'Menyimpan...');const mode=$('#evCostMode',root).value;const{data:id,error}=await state.supabase.rpc('admin_create_event',{p_event_name:$('#evName',root).value.trim(),p_customer_name:$('#evCustomer',root).value.trim(),p_customer_phone:$('#evPhone',root).value.trim()||null,p_event_date:$('#evDate',root).value,p_location:$('#evLocation',root).value.trim()||null,p_quantity:Number($('#evQty',root).value),p_unit_price:Number($('#evPrice',root).value),p_delivery_charge:Number($('#evDeliveryIncome',root).value||0),p_notes:$('#evNotes',root).value.trim()||null,p_status:$('#evStatus',root).value,p_base_cost_mode:mode,p_base_unit_cost:mode==='combined'?Number($('#evBaseCost',root).value||0):0});setBusy(b,false);if(error)return toast(error.message,'error');toast('Event berjaya disimpan.','success');state.eventSelectedId=id;state.currentView='event-detail';await adminEventDetail(root,id);});
    $('#eventFilterForm',root).addEventListener('submit',async ev=>{ev.preventDefault();state.eventFilters={search:$('#eventSearch',root).value.trim(),start:$('#eventStart',root).value,end:$('#eventEnd',root).value,status:$('#eventStatus',root).value};await adminEvents(root);});
    $$('.event-open',root).forEach(b=>b.addEventListener('click',async()=>{state.eventSelectedId=b.dataset.id;state.currentView='event-detail';await adminEventDetail(root,b.dataset.id);}));
  }

  async function adminEventDetail(root,id){
    if(!id)return adminEvents(root);
    const [{data,error},{data:materials,error:me}]=await Promise.all([state.supabase.rpc('admin_event_detail',{p_event_id:id}),state.supabase.from('materials').select('id,name,unit,current_qty,avg_unit_cost,active').eq('active',true).order('name')]);if(error)throw error;if(me)throw me;if(!data)throw new Error('Event tidak ditemui atau akses ditolak.');
    const e=data.event||{},payments=data.payments||[],costs=data.costs||[],usage=data.stock_usage||[],commissions=data.commissions||[],partners=data.partners||[];
    const drink=Number(e.quantity||0)*Number(e.unit_price||0),income=drink+Number(e.customer_delivery_charge||0),received=sum(payments),balance=income-received,base=e.base_cost_mode==='combined'?Number(e.quantity||0)*Number(e.base_unit_cost||0):0,usageCost=e.base_cost_mode==='itemized'?sum(usage):0,totalCost=base+usageCost+sum(costs),totalComm=sum(commissions),net=income-totalCost-totalComm,paidComm=sum(commissions.filter(x=>x.status==='paid')),pendingComm=totalComm-paidComm;
    const matOpts=(materials||[]).map(m=>`<option value="${m.id}">${esc(m.name)} — stok ${num(m.current_qty,4)} ${esc(m.unit)} • avg ${money(m.avg_unit_cost)}</option>`).join('');
    root.innerHTML=pageHead('EVENT','Butiran Event',`${e.event_name} • ${e.customer_name}`,`<button class="btn ghost" id="eventBack">← Senarai Event</button>`)+`
      <div class="kpi-grid">${kpi('Jualan Minuman',money(drink))}${kpi('Delivery Pelanggan',money(e.customer_delivery_charge))}${kpi('Jumlah Pendapatan',money(income))}${kpi('Bayaran Diterima',money(received))}${kpi('Baki Pelanggan',money(balance))}${kpi('Jumlah Kos',money(totalCost))}${kpi('Komisen',money(totalComm),`Paid ${money(paidComm)} • Pending ${money(pendingComm)}`)}${kpi(net>=0?'Untung Bersih':'Kerugian',`<span class="${net>=0?'profit-positive':'profit-negative'}">${money(net)}</span>`)}</div>
      <section class="panel"><div class="panel-head"><div><h3>Maklumat Tempahan</h3><p>Produk 100ml • chilled. Harga event ialah snapshot dan tidak berubah bila kadar lalai berubah.</p></div></div><form id="eventEditForm" class="form-stack"><div class="grid-2"><label>Nama event<input id="edName" value="${esc(e.event_name)}" required></label><label>Pelanggan<input id="edCustomer" value="${esc(e.customer_name)}" required></label><label>Telefon<input id="edPhone" value="${esc(e.customer_phone||'')}"></label><label>Tarikh<input id="edDate" type="date" value="${esc(e.event_date)}" required></label><label>Lokasi<input id="edLocation" value="${esc(e.location||'')}"></label><label>Kuantiti<input id="edQty" type="number" min="200" value="${e.quantity}" required></label><label>Harga / botol RM<input id="edPrice" type="number" min="0" step="0.01" value="${Number(e.unit_price).toFixed(2)}"></label><label>Caj delivery pelanggan RM<input id="edDelivery" type="number" min="0" step="0.01" value="${Number(e.customer_delivery_charge||0).toFixed(2)}"></label><label>Status<select id="edStatus">${Object.entries(statusLabel).map(([v,l])=>`<option value="${v}" ${e.status===v?'selected':''}>${l}</option>`).join('')}</select></label><label>Kaedah kos<select id="edCostMode"><option value="combined" ${e.base_cost_mode==='combined'?'selected':''}>Gabungan bahan + botol</option><option value="itemized" ${e.base_cost_mode==='itemized'?'selected':''}>Itemized / penggunaan stok</option></select></label><label>Kos gabungan / botol RM<input id="edBaseCost" type="number" min="0" step="0.0001" value="${Number(e.base_unit_cost||0).toFixed(4)}" ${e.base_cost_mode==='itemized'?'disabled':''}></label></div><label>Catatan<textarea id="edNotes" rows="3">${esc(e.notes||'')}</textarea></label><button class="btn primary" type="submit">Update Event</button></form></section>
      <div class="grid-2">
        <section class="panel"><div class="panel-head"><div><h3>Bayaran Pelanggan</h3><p>Kutipan ≠ keuntungan. Baki dikira daripada jumlah pendapatan event.</p></div></div><form id="eventPaymentForm" class="form-stack"><label>Jumlah RM<input id="payAmount" type="number" min="0.01" step="0.01" required></label><label>Tarikh / masa<input id="payDate" type="datetime-local" value="${isoLocal(new Date())}"></label><label>Nota<input id="payNotes"></label><button class="btn primary" type="submit">Tambah Bayaran</button></form><div class="table-wrap"><table class="data-table"><thead><tr><th>Tarikh</th><th>Nota</th><th class="num">Jumlah</th><th></th></tr></thead><tbody>${payments.length?payments.map(x=>`<tr><td>${dateMY(x.paid_at)}</td><td>${esc(x.notes||'-')}</td><td class="num">${money(x.amount)}</td><td><button class="btn sm danger pay-delete" data-id="${x.id}">Delete</button></td></tr>`).join(''):tableEmpty(4)}</tbody></table></div></section>
        <section class="panel"><div class="panel-head"><div><h3>Kos Event</h3><p>${e.base_cost_mode==='combined'?`Kos gabungan: ${num(e.quantity)} × ${money(e.base_unit_cost)} = ${money(base)}.`:'Kos stocked item boleh datang daripada “Penggunaan Stok” di bawah.'}</p></div></div><form id="eventCostForm" class="form-stack"><label>Kategori<select id="costCategory">${Object.entries(costLabel).map(([v,l])=>`<option value="${v}" ${(e.base_cost_mode==='combined'&&(v==='beverage_material'||v==='bottle'))?'disabled':''}>${l}</option>`).join('')}</select></label><label>Keterangan<input id="costDesc"></label><label>Jumlah RM<input id="costAmount" type="number" min="0" step="0.01" required></label><button class="btn primary" type="submit">Tambah Kos</button></form><div class="table-wrap"><table class="data-table"><thead><tr><th>Kategori</th><th>Keterangan</th><th class="num">Jumlah</th><th></th></tr></thead><tbody>${costs.length?costs.map(x=>`<tr><td>${esc(costLabel[x.category]||x.category)}</td><td>${esc(x.description||'-')}</td><td class="num">${money(x.amount)}</td><td><button class="btn sm ghost cost-edit" data-id="${x.id}" data-category="${esc(x.category)}" data-description="${esc(x.description||'')}" data-amount="${x.amount}">Edit</button> <button class="btn sm danger cost-delete" data-id="${x.id}">Delete</button></td></tr>`).join(''):tableEmpty(4)}</tbody></table></div></section>
      </div>
      <section class="panel"><div class="panel-head"><div><h3>Penggunaan Stok Untuk Event</h3><p>Pembelian penuh kekal dalam Belian Stok. Di sini hanya kuantiti sebenar yang digunakan ditolak daripada inventory. Dalam mode Itemized, kos snapshot penggunaan ini masuk kos event; dalam mode Gabungan, stok tetap ditolak tetapi kos tidak ditolak kali kedua.</p></div></div><form id="eventStockForm" class="inline-form"><div class="field"><label>Material / stok</label><select id="stockMaterial">${matOpts}</select></div><div class="field"><label>Quantity digunakan</label><input id="stockQty" type="number" min="0.0001" step="0.0001" required></div><div class="field"><label>Nota</label><input id="stockNotes" placeholder="Contoh: botol 100ml"></div><button class="btn primary" type="submit">Rekod Penggunaan</button></form><div class="table-wrap"><table class="data-table"><thead><tr><th>Stok</th><th class="num">Qty</th><th class="num">Unit Cost Snapshot</th><th class="num">Nilai Digunakan</th><th></th></tr></thead><tbody>${usage.length?usage.map(x=>`<tr><td><strong>${esc(x.material_name)}</strong><br><span class="muted">${esc(x.notes||'')}</span></td><td class="num">${num(x.quantity,4)} ${esc(x.unit)}</td><td class="num">${money(x.unit_cost_snapshot)}</td><td class="num">${money(x.amount)}</td><td><button class="btn sm danger stock-remove" data-id="${x.id}">Undo</button></td></tr>`).join(''):tableEmpty(5)}</tbody></table></div></section>
      <section class="panel"><div class="panel-head"><div><h3>Komisen / Upah Event</h3><p>Kadar awal RM0.80/botol. Ini berasingan daripada Rider RM1.80 dan Ejen RM1.20. Pending tetap ditolak daripada untung.</p></div></div><form id="eventCommissionForm" class="inline-form"><div class="field"><label>Penerima</label><input id="commName" required></div><div class="field"><label>Kadar / botol RM</label><input id="commRate" type="number" min="0" step="0.01" value="0.80"></div><div class="field"><label>Qty</label><input id="commQty" type="number" min="1" value="${e.quantity}"></div><div class="field"><label>Nota</label><input id="commNotes"></div><button class="btn primary" type="submit">Tambah Komisen</button></form><div class="table-wrap"><table class="data-table"><thead><tr><th>Penerima</th><th class="num">Rate</th><th class="num">Qty</th><th class="num">Jumlah</th><th>Status</th><th></th></tr></thead><tbody>${commissions.length?commissions.map(x=>`<tr><td><strong>${esc(x.recipient_name)}</strong><br><span class="muted">${esc(x.notes||'')}</span></td><td class="num">${money(x.rate_per_bottle)}</td><td class="num">${num(x.quantity)}</td><td class="num">${money(x.amount)}</td><td>${x.status==='paid'?'Sudah dibayar':'Belum dibayar'}</td><td><button class="btn sm ghost comm-edit" data-id="${x.id}" data-name="${esc(x.recipient_name)}" data-rate="${x.rate_per_bottle}" data-qty="${x.quantity}" data-notes="${esc(x.notes||'')}">Edit</button> <button class="btn sm ghost comm-toggle" data-id="${x.id}" data-status="${x.status==='paid'?'pending':'paid'}">${x.status==='paid'?'Belum Bayar':'Sudah Bayar'}</button> <button class="btn sm danger comm-delete" data-id="${x.id}">Delete</button></td></tr>`).join(''):tableEmpty(6)}</tbody></table></div></section>
      <section class="panel"><div class="panel-head"><div><h3>Pembahagian Partner 50 / 50</h3><p>Pembayaran partner bukan kos event. Ia distribution selepas untung bersih dikira.</p></div></div>${net<0?`<div class="empty-state"><strong class="profit-negative">Kerugian ${money(Math.abs(net))}</strong><br>Tiada keuntungan untuk diagihkan.</div>`:''}<div class="table-wrap"><table class="data-table"><thead><tr><th>Partner</th><th>Share</th><th class="num">Jumlah</th><th>Status</th><th>Tarikh</th><th></th></tr></thead><tbody>${partners.length?partners.map(x=>`<tr><td><strong>${esc(x.partner_name)}</strong></td><td>${num(x.share_percent,2)}%</td><td class="num">${money(x.amount)}</td><td>${x.status==='paid'?'Sudah dibayar':'Belum dibayar'}</td><td>${dateMY(x.paid_at)}</td><td><button class="btn sm ghost partner-toggle" data-id="${x.id}" data-status="${x.status==='paid'?'pending':'paid'}" ${Number(x.amount)<=0?'disabled':''}>${x.status==='paid'?'Belum Bayar':'Sudah Bayar'}</button></td></tr>`).join(''):tableEmpty(6)}</tbody></table></div><div class="muted">Jumlah bahagian ${money(sum(partners))} • Untung boleh diagih ${money(Math.max(net,0))}</div></section>`;

    $('#eventBack',root).addEventListener('click',async()=>{state.currentView='events';await adminEvents(root);});
    $('#edCostMode',root).addEventListener('change',()=>$('#edBaseCost',root).disabled=$('#edCostMode',root).value==='itemized');
    $('#eventEditForm',root).addEventListener('submit',async ev=>{ev.preventDefault();const b=ev.submitter,mode=$('#edCostMode',root).value;setBusy(b,true,'Updating...');const{error}=await state.supabase.rpc('admin_update_event',{p_event_id:id,p_event_name:$('#edName',root).value.trim(),p_customer_name:$('#edCustomer',root).value.trim(),p_customer_phone:$('#edPhone',root).value.trim()||null,p_event_date:$('#edDate',root).value,p_location:$('#edLocation',root).value.trim()||null,p_quantity:Number($('#edQty',root).value),p_unit_price:Number($('#edPrice',root).value),p_delivery_charge:Number($('#edDelivery',root).value||0),p_notes:$('#edNotes',root).value.trim()||null,p_status:$('#edStatus',root).value,p_base_cost_mode:mode,p_base_unit_cost:mode==='combined'?Number($('#edBaseCost',root).value||0):0});setBusy(b,false);if(error)return toast(error.message,'error');toast('Event dikemas kini.','success');await adminEventDetail(root,id);});
    $('#eventPaymentForm',root).addEventListener('submit',async ev=>{ev.preventDefault();const b=ev.submitter;setBusy(b,true);const{error}=await state.supabase.rpc('admin_add_event_payment',{p_event_id:id,p_amount:Number($('#payAmount',root).value),p_paid_at:$('#payDate',root).value?new Date($('#payDate',root).value).toISOString():new Date().toISOString(),p_notes:$('#payNotes',root).value.trim()||null});setBusy(b,false);if(error)return toast(error.message,'error');toast('Bayaran direkod.','success');await adminEventDetail(root,id);});
    $('#eventCostForm',root).addEventListener('submit',async ev=>{ev.preventDefault();const b=ev.submitter;setBusy(b,true);const{error}=await state.supabase.rpc('admin_add_event_cost',{p_event_id:id,p_category:$('#costCategory',root).value,p_description:$('#costDesc',root).value.trim()||null,p_amount:Number($('#costAmount',root).value),p_incurred_at:new Date().toISOString()});setBusy(b,false);if(error)return toast(error.message,'error');toast('Kos ditambah.','success');await adminEventDetail(root,id);});
    $('#eventStockForm',root).addEventListener('submit',async ev=>{ev.preventDefault();const b=ev.submitter;setBusy(b,true);const{error}=await state.supabase.rpc('admin_add_event_material_usage',{p_event_id:id,p_material_id:$('#stockMaterial',root).value,p_quantity:Number($('#stockQty',root).value),p_notes:$('#stockNotes',root).value.trim()||null});setBusy(b,false);if(error)return toast(error.message,'error');toast('Penggunaan stok direkod.','success');await adminEventDetail(root,id);});
    $('#eventCommissionForm',root).addEventListener('submit',async ev=>{ev.preventDefault();const b=ev.submitter;setBusy(b,true);const{error}=await state.supabase.rpc('admin_add_event_commission',{p_event_id:id,p_recipient_name:$('#commName',root).value.trim(),p_rate:Number($('#commRate',root).value),p_quantity:Number($('#commQty',root).value),p_notes:$('#commNotes',root).value.trim()||null});setBusy(b,false);if(error)return toast(error.message,'error');toast('Komisen ditambah.','success');await adminEventDetail(root,id);});
    $$('.pay-delete',root).forEach(b=>b.addEventListener('click',async()=>{if(!await confirmAction('Delete bayaran?','Rekod bayaran pelanggan ini akan dipadam.'))return;const{error}=await state.supabase.rpc('admin_delete_event_payment',{p_payment_id:b.dataset.id});if(error)return toast(error.message,'error');await adminEventDetail(root,id);}));
    $$('.stock-remove',root).forEach(b=>b.addEventListener('click',async()=>{if(!await confirmAction('Undo penggunaan stok?','Quantity akan dipulangkan ke stok bahan.'))return;const{error}=await state.supabase.rpc('admin_remove_event_material_usage',{p_usage_id:b.dataset.id});if(error)return toast(error.message,'error');toast('Stok dipulangkan.','success');await adminEventDetail(root,id);}));
    $$('.cost-edit',root).forEach(b=>b.addEventListener('click',async()=>{const a=window.prompt('Jumlah kos RM:',b.dataset.amount);if(a===null)return;const d=window.prompt('Keterangan:',b.dataset.description||'');if(d===null)return;const{error}=await state.supabase.rpc('admin_update_event_cost',{p_cost_id:b.dataset.id,p_category:b.dataset.category,p_description:d||null,p_amount:Number(a)});if(error)return toast(error.message,'error');await adminEventDetail(root,id);}));
    $$('.cost-delete',root).forEach(b=>b.addEventListener('click',async()=>{if(!await confirmAction('Delete kos?','Kos ini akan dikeluarkan daripada event.'))return;const{error}=await state.supabase.rpc('admin_delete_event_cost',{p_cost_id:b.dataset.id});if(error)return toast(error.message,'error');await adminEventDetail(root,id);}));
    $$('.comm-edit',root).forEach(b=>b.addEventListener('click',async()=>{const n=window.prompt('Nama penerima:',b.dataset.name);if(n===null)return;const r=window.prompt('Kadar RM/botol:',b.dataset.rate);if(r===null)return;const q=window.prompt('Quantity:',b.dataset.qty);if(q===null)return;const notes=window.prompt('Catatan:',b.dataset.notes||'');if(notes===null)return;const{error}=await state.supabase.rpc('admin_update_event_commission',{p_commission_id:b.dataset.id,p_recipient_name:n,p_rate:Number(r),p_quantity:Number(q),p_notes:notes||null});if(error)return toast(error.message,'error');await adminEventDetail(root,id);}));
    $$('.comm-delete',root).forEach(b=>b.addEventListener('click',async()=>{if(!await confirmAction('Delete komisen?','Komisen ini akan dikeluarkan daripada event.'))return;const{error}=await state.supabase.rpc('admin_delete_event_commission',{p_commission_id:b.dataset.id});if(error)return toast(error.message,'error');await adminEventDetail(root,id);}));
    $$('.comm-toggle',root).forEach(b=>b.addEventListener('click',async()=>{const{error}=await state.supabase.rpc('admin_set_event_commission_status',{p_commission_id:b.dataset.id,p_status:b.dataset.status,p_paid_at:b.dataset.status==='paid'?new Date().toISOString():null});if(error)return toast(error.message,'error');await adminEventDetail(root,id);}));
    $$('.partner-toggle',root).forEach(b=>b.addEventListener('click',async()=>{const{error}=await state.supabase.rpc('admin_set_event_partner_status',{p_share_id:b.dataset.id,p_status:b.dataset.status,p_paid_at:b.dataset.status==='paid'?new Date().toISOString():null});if(error)return toast(error.message,'error');await adminEventDetail(root,id);}));
  }
})();